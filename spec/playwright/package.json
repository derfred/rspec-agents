{
  "name": "rspec-agents-playwright-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "bundle exec ruby generate_fixtures.rb",
    "test": "playwright test",
    "test:ui": "bundle exec ruby generate_fixtures.rb && playwright test --ui",
    "test:debug": "bundle exec ruby generate_fixtures.rb && playwright test --debug",
    "generate-fixtures": "bundle exec ruby generate_fixtures.rb"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
